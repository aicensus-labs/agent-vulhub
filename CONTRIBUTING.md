# 新增漏洞环境

1. 核对官方 CVE/GHSA、受影响版本和修复来源。重新判断 Agent 关联性，不直接照搬现有数据库的自动标签。
2. 使用 `python3 -m runner new <product> <CVE-ID>` 创建草稿。产品目录用小写字母、数字和连字符，CVE 用大写规范编号。
3. 填写 `metadata.toml`、README、镜像配方和 fixtures。保留第三方材料原始链接及许可。模板中的占位字符串必须替换。
4. 实现 `reproduce.py` 和 `verify.py`；需要真实模型时另实现 `end_to_end.py`。若不适用端到端模型测试，记录原因，不伪造测试结果。
5. 在独立实验环境内测试漏洞版、修复版、正常任务；记录退出状态之外的实际效果证据。
6. 更新对应验证层的状态、日期及证据引用。没有可复核证据时保持 `not_run` 或 `failed`。
7. 运行仓库检查、工具测试，再提交明确的环境目录和索引变更。

`draft` 表示未完成；`ready` 表示元数据达到收录要求，不自动意味着所有平台都复现成功。当前静态检查不审计脚本行为、不验证公告真实性，也不证明 Docker 隔离有效。

涉及宿主逃逸的案例使用独立 VM；容器内的“工作区外”测试应指向 VM/容器内部的模拟目录。不要把真实主机目录当作攻击目标。
