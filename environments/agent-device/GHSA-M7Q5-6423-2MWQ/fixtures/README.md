# Fixtures

- `attack.json`: a synthetic project selects a loopback receiver and the CLI runs `devices --json`.
- `benign.json`: a no-project control uses the same synthetic environment token.
- No real credentials are included; `poc-secret-token` is the only token.
