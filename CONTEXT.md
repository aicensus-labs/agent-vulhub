# Agent 漏洞复现

本仓库以稳定、可重复的漏洞机制复现为目标。机制复现与真实模型端到端复现是不同的验证概念。

## Language

**机制复现（mechanism）**：
通过真实漏洞代码路径触发并验证漏洞效果的实验，允许直接调用内部函数、固定模型输出或预先建立必要状态。它证明漏洞机制成立，不证明真实模型会被攻击材料诱导。
_Avoid_：真实模型端到端复现、仅凭退出码认定复现成功

**真实模型端到端复现（end_to_end）**：
真实模型读取攻击材料并参与形成完整攻击链的实验。它不是本仓库每个 CVE 必须达到的复现目标。
_Avoid_：固定模型输出的机制复现

**Agent-PoC 任务（agent_poc task）**：
提供给外部 Agent 的受控任务包，默认包含漏洞版完整源码、agent-facing 漏洞描述和候选提交约定，不包含修复版、reference PoC、验证器或预期结论。它是候选生成输入，不等同于机制复现结果。
_Avoid_：把 patched 源码、verifier 或预期 canary 混入 Level 1 任务包

**候选 PoC（candidate PoC）**：
Agent 提交给统一 Candidate runner 的脚本、输入文件或请求生成器。候选退出码只表示执行状态，漏洞结论必须由真实产品效果和独立验证器给出。
_Avoid_：候选输出 `success=true` 或自写 verdict 作为漏洞证据

**Agent-PoC runner**：
负责打包公开任务、接收候选、在隐藏的 vulnerable/patched 变体中执行四个场景、收集事实、调用验证器和清理资源的统一运行器。环境只提供候选格式和目标效果适配信息。
_Avoid_：每个 CVE 自定义完整的 Agent 执行生命周期

**基准机制 PoC（reference mechanism PoC）**：
维护者编写并固定在环境中的 `reproduce.py`，用于证明真实上游代码路径和目标效果成立；它不代表 Agent 已生成 PoC，也不自动计入 Agent-PoC 成功率。
_Avoid_：用维护者 PoC 冒充模型生成结果

**上游版本复现**：
以固定源码版本及其依赖运行目标产品，并在该实现中触发漏洞机制。触发入口可以是产品内部函数，不要求始终经过完整外部界面。
_Avoid_：抽取漏洞代码的最小模拟程序

**三场景验收**：
一次复现必须分别验证漏洞版产生预期效果、修复版阻断相同输入，以及正常任务在两版均可完成。三类结果共同构成可通过的证据。
_Avoid_：只运行攻击场景、只看进程退出码

**实验隔离**：
复现实验默认在受限 Docker 网络和权限边界内运行，不能把宿主机资源当作攻击目标。需要突破默认边界的案例必须给出明确、最小化且可审计的理由。
_Avoid_：默认特权容器、宿主机目录作为测试目标

**复现脚本（reproduce.py）**：
在固定的上游版本中执行漏洞触发步骤，可以使用固定输入或直接调用内部函数，并输出可供验证器检查的事实证据。它相当于机制层 PoC，不单独决定最终是否通过。
_Avoid_：只打印“成功”、把退出码当作漏洞证据

**验证器（verify.py）**：
独立读取复现实验产生的结构化结果和受控证据，检查漏洞版效果、修复版阻断及正常任务条件，并据此给出场景和整体结论。它不负责再次触发漏洞。
_Avoid_：复现脚本的别名、依赖攻击脚本内部布尔值

**不可变实验镜像**：
由仓库内配方从固定源码和基础镜像构建，并以 content digest 标识的漏洞版或修复版镜像。复现记录引用 digest，而不是可变的标签。
_Avoid_：只记录 `latest` 等浮动 tag

**标准复现命令**：
由仓库运行器提供、对任一环境执行构建检查、三场景实验、证据验证和清理的统一入口。环境脚本实现具体漏洞逻辑，运行器不替代它们。
_Avoid_：每个 CVE 自定义一套无法自动编排的命令

**复现证据**：
实验产生的、可独立核查的效果记录，例如受控文件、响应或日志。证据描述实际发生的事实，验证结论由验证器依据这些事实形成。
_Avoid_：PoC 自报成功、仅凭截图或退出码认定通过

**无害效果（safe effect）**：
在隔离容器内由漏洞触发产生的、可验证且可逆的 canary 结果，例如受控文件、模拟记录或本地假数据。它用于证明能力边界失效，不接触真实凭据、宿主机资源或外部目标。
_Avoid_：真实数据外传、破坏宿主机、用危险载荷证明漏洞

**固定 fixture**：
纳入环境并可用内容哈希复核的攻击输入、正常输入、模拟数据和固定模型输出。复现实验不临时从网络获取未锁定的输入，随机性与所需环境变量必须显式确定。
_Avoid_：运行时抓取攻击材料、未记录的随机输入

**就绪环境（ready）**：
满足收录要求且同一组固定镜像在全新实验状态下连续通过 3 轮完整三场景验收的环境。上游修复可以是尚未正式发布的固定 commit。
_Avoid_：仅元数据完整、仅漏洞版触发成功

**已验证平台**：
明确记录且完成规定验收的运行平台组合。环境的就绪结论仅适用于已验证平台，不能自动推广到其他架构或 Docker 运行环境。
_Avoid_：支持所有 Docker 平台

**服务就绪**：
目标服务及其依赖已通过声明的健康检查并满足 PoC 的启动前提。就绪失败、启动崩溃、实验超时和验证失败是不同的结果，不能互相替代。
_Avoid_：容器进程存在就视为服务可用

**失败记录**：
对未通过或未完成实验的可复核说明，至少指出阶段、状态、预期与实际、退出码（如有）、证据引用和脱敏日志位置。失败记录不能只写一个布尔值，也不能把基础设施失败解释成修复阻断。
_Avoid_：空的 `failed`、只保存成功日志

**草稿环境（draft）**：
尚未满足就绪要求的复现环境，可保留已完成的实验及其证据。缺少上游修复对照的环境仍属于草稿。
_Avoid_：已通过完整验收的环境

**维护者晋升**：
由维护者审阅来源、配方、触发器、验证器和三轮证据后，将环境从 `draft` 标记为 `ready` 的动作。影响实验输入或执行逻辑的变更会使环境重新回到 `draft`。
_Avoid_：作者自报通过、代码变更后沿用旧 ready

**实验工具链**：
生成复现结果的 runner、Docker Engine、Compose、宿主平台和镜像 digest 的完整版本组合。结果必须绑定工具链，材料性变化会使旧验收证据失效。
_Avoid_：只记录应用版本、忽略运行器和容器工具版本

**证据治理**：
`ready` 证据由维护者复核并长期保留脱敏摘要和 manifest；涉及隔离例外时需要额外审阅者。原始日志可以作为外部 artifact 保存，撤回或错误通过降级和追加记录处理。
_Avoid_：作者单独自证、删除撤回记录
