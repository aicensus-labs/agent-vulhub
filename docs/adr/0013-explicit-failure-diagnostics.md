# Preserve Explicit Failure Diagnostics

运行器使用 `0`（全部通过）、`1`（实验失败）、`2`（未运行或前置条件不足）和 `3`（基础设施错误）区分结果，并为每个非通过结果保存失败阶段、分类、预期与实际、退出码、脱敏日志和证据引用。实验默认不自动重试，三轮尝试及失败记录完整保留，避免把未就绪或基础设施问题误判为漏洞/修复结论。

**Status**: accepted
