# Require Maintainer Promotion and Invalidate Stale Evidence

环境作者只能提交 `draft`；维护者审阅来源、构建配方、PoC、验证器和三轮证据后才能标记 `ready`。源码、镜像、配方、fixture、执行脚本、验收规则或运行器版本的材料性变化会自动将环境降回 `draft` 并重置验证状态，确保 `ready` 与一组明确的实验输入和工具链绑定。

**Status**: accepted
