schema_version = 1
id = "{{PRODUCT}}/{{CVE}}"
cve = "{{CVE}}"
product = "{{PRODUCT}}"
title = "TODO: verified vulnerability title"
lifecycle = "draft"
agent_specificity = "unclassified"
advisories = []
description = "TODO: root cause and trust boundary"
prerequisites = []

[vulnerable]
version = ""
source_url = ""
commit = ""
image = ""
archive = "vulnerable-source.tar.gz"

[patched]
version = ""
source_url = ""
commit = ""
image = ""
archive = "patched-source.tar.gz"

[build]
base_image = ""
dockerfile = "Dockerfile"
# Each entry: { name = "...", url = "https://...", sha256 = "..." }
# Include source archives AND every dependency needed by network-disabled builds.
inputs = []

[runtime]
isolation = "container"
platforms = ["linux/amd64"]
mode = "oneshot"
exceptions = []
requires_model = false
network_requirements = "TODO: required destinations or no network"

[verification.mechanism]
status = "not_run"
verified_at = ""
evidence = []
notes = ""

[verification.end_to_end]
status = "not_applicable"
verified_at = ""
evidence = []
notes = "Mechanism reproduction is required; real-model testing is optional."
